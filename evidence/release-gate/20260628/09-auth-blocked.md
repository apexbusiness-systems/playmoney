# Auth & Route Gates

OTP Capture: BLOCKED (No configured test inbox or `RESEND_API_KEY` available in this environment).
Test User Created: `playmoneywins@gmail.com`
Status: Admin role applied to user metadata directly via the database, bypassing OTP UI.
Magic Link Auth: Used to establish a live Chrome session for downstream testing.
Note: The OTP UI itself cannot be verified working end-to-end until tested with a live email inbox.
